{#-
  stakeholder_summary.md.j2

  Deterministic stakeholder summary for the CRUX audit dossier.
  Replaces the LLM-generated summary that earlier versions of this project
  delegated to Granite via watsonx.ai. Determinism is the feature — running
  the same notebook through CRUX twice must produce byte-identical output,
  because the dossier is a CI artifact and reproducibility is the contract.

  Expected context (built by mcp_server/dossier.py):
    notebook_name           : str          e.g. "02_messy.ipynb"
    audit_timestamp         : str (ISO8601)
    crux_version            : str          e.g. "0.1.0"
    verdict                 : str          one of: CLEAN | READY_WITH_DECISIONS | BLOCKED
    cells                   : dict
      total, load_bearing, scaffolding, exploratory, dead   (all ints)
    gaps                    : dict
      total, blockers, auto_patched, decisions_required, dismissed   (ints)
    blockers                : list[ {id:int, name:str, location:str} ]
    decisions               : list[ {id:int, name:str, options:int} ]
    auto_patched_gaps       : list[ {id:int, name:str} ]
    top_recommendation      : str | None   (filled by dossier builder for the most severe issue)
-#}
# Stakeholder Summary — `{{ notebook_name }}`

*Generated by CRUX {{ crux_version }} at {{ audit_timestamp }} · deterministic, reproducible*

---

## Verdict

{% if verdict == "CLEAN" -%}
**Ready to ship.** No blockers, no decisions outstanding. The recovered service from `{{ notebook_name }}` passed all {{ gaps.total + gaps.auto_patched }} production checks. Merge whenever convenient.
{%- elif verdict == "READY_WITH_DECISIONS" -%}
**Ready to ship after {{ gaps.decisions_required }} {% if gaps.decisions_required == 1 %}decision is{% else %}decisions are{% endif %} resolved.** No blockers were detected, but {{ gaps.decisions_required }} production {% if gaps.decisions_required == 1 %}gap requires{% else %}gaps require{% endif %} a human choice between two or three valid alternatives. CRUX has logged the options; pick one and re-run the audit.
{%- else -%}
**Not ready to ship.** {{ gaps.blockers }} critical {% if gaps.blockers == 1 %}blocker is{% else %}blockers are{% endif %} present and must be resolved before this service can be deployed. {% if gaps.decisions_required > 0 %}A further {{ gaps.decisions_required }} {% if gaps.decisions_required == 1 %}decision is{% else %}decisions are{% endif %} also outstanding.{% endif %} The CI gate will block any merge attempt while blockers remain.
{%- endif %}

---

## What was in the notebook

`{{ notebook_name }}` contained **{{ cells.total }} cells**.
After narrative-intent recovery, CRUX classified them as:

- **{{ cells.load_bearing }}** load-bearing — code that flows into the final model artifact and ships in the recovered service
- **{{ cells.scaffolding }}** scaffolding — setup, configuration, and helper code that supports the load-bearing path but is reorganized in the service
- **{{ cells.exploratory }}** exploratory — alternative approaches the author considered and rejected
- **{{ cells.dead }}** dead — assignments and side-effects with no consumer downstream of `model.fit()`

{% set kept = cells.load_bearing + cells.scaffolding -%}
{% set removed = cells.exploratory + cells.dead -%}
The recovered service is built from {{ kept }} of {{ cells.total }} cells ({{ ((kept / cells.total) * 100) | round(0) | int }}% retained). Every reclassification is cited line-by-line in the decision log below.

---

## What the audit found

CRUX checked the recovered service against 15 production gaps. Results:

| Disposition | Count |
|---|---|
| Auto-patched | {{ gaps.auto_patched }} |
| Decision required | {{ gaps.decisions_required }} |
| Blocker | {{ gaps.blockers }} |
| Dismissed (not applicable) | {{ gaps.dismissed }} |
| **Total surfaced** | **{{ gaps.total }}** |

{% if blockers -%}
### Blockers ({{ blockers | length }})

These must be resolved before merge:

{% for blocker in blockers -%}
- **Gap #{{ blocker.id }} — {{ blocker.name }}** at `{{ blocker.location }}`
{% endfor %}
{%- endif %}

{% if decisions -%}
### Decisions outstanding ({{ decisions | length }})

These have multiple valid resolutions; a human must choose:

{% for decision in decisions -%}
- **Gap #{{ decision.id }} — {{ decision.name }}** ({{ decision.options }} options logged)
{% endfor %}
{%- endif %}

{% if auto_patched_gaps -%}
### Auto-patched ({{ auto_patched_gaps | length }})

CRUX applied safe, deterministic fixes for these. Review the diff in the recovered service:

{% for patch in auto_patched_gaps -%}
- Gap #{{ patch.id }} — {{ patch.name }}
{% endfor %}
{%- endif %}

---

## Recommendation

{% if verdict == "CLEAN" -%}
Proceed to merge. The recovered service passed every production check that applies to this notebook. Future-you will thank past-you for the audit trail.
{%- elif verdict == "READY_WITH_DECISIONS" -%}
Resolve the {{ gaps.decisions_required }} outstanding {% if gaps.decisions_required == 1 %}decision{% else %}decisions{% endif %} listed above by choosing among the logged options, then re-run `crux audit {{ notebook_name }}`. The CI gate will pass once decisions are recorded.
{%- else -%}
{% if top_recommendation -%}
{{ top_recommendation }}
{%- else -%}
Address the {{ gaps.blockers }} {% if gaps.blockers == 1 %}blocker{% else %}blockers{% endif %} above first — they are categorized as critical because shipping with them risks data loss, security exposure, or silent inference failure. Once they are resolved, re-run the audit and triage any remaining decisions.
{%- endif %}
{%- endif %}

---

*This summary is generated from the structured audit result by a deterministic Jinja2 template (`templates/stakeholder_summary.md.j2`). No language model was involved. Running CRUX twice on the same notebook will produce byte-identical output. The full audit dossier — with cell-level citations, decision options, and patch diffs — is attached as `dossier.html`.*
